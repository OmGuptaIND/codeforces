#include <bits/stdc++.h>
using namespace std;

string dividingChocolate(vector<int>& chocolates, int n){
    int 
    for(int i = 0; i<n; i++){
        if()
    }
}


int main(){
    int tests;
    cin >> tests;
    for (int test = 0; test < tests; test++)
    {
        int n;
        cin >> n;
        vector<int> chocolates(n);
        for (int i = 0; i < n; i++)
        {
            cin >> chocolates[i];
        }
        string result = dividingChocolate(chocolates, n);
        cout << result << endl;
    }
}